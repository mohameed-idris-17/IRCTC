# IRCTC
IRCTC Train Ticket Booking – Java console app to sign up, log in, search trains, view seats, book tickets, and cancel bookings. Data persists in JSON via Jackson; passwords hashed with BCrypt. Run with Gradle (gradle run) or VS Code. Includes services, entities, and local DB.
